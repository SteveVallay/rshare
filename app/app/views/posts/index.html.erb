<h1>All files</h1>


<p>
<%= link_to "Upload" , new_post_path %>
</p>
<table class="table">
  <thead>
    <tr>
        <th>Title</th>
        <th>Description</th>
        <th>file name</th>
        <th>detail</th>
    </tr>
  </thead>
   <% @posts.each do |post| %>
   <tr>
       <td><%= post.title %></td>
       <td><%= post.text %></td>
       <td><%= post.origin_name%></td>
       <td><%= link_to 'Show', post_path(post) %></td>
   </tr>
   <% end %>
</table>
