<%= form_for(@book) do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :select_file %><br>
    <%= f.file_field :file %>
  </div>
  <div class="field">
    <%= f.label "Name:" %>
    <%= f.text_field :name ,class: "book-name-input"%>
  </div>
  <div class="field">
    <%= f.label "Desc:" %><br>
    <%= f.text_area :desc ,class: "book-desc-input"%>
  </div>
  <div class="field">
    <%= f.label "Image:" %><br>
    <%= f.text_field :image,class: "book-image-input"%>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-primary"%>
  </div>
<% end %>
